---
description: 숏츠 콘텐츠 제작 파이프라인 실행. 소재 입력 → 주제 → 대본 → 검수 → TTS → 저장까지 자동.
---

숏츠 콘텐츠를 자동으로 생성한다. 사용자에게 중간 승인을 묻지 않는다. 최종 결과만 보고한다.

## 실행 절차

### 0. 서버 확인
`curl -s -o /dev/null -w "%{http_code}" http://localhost:8000` → 200이 아니면 중단.

### 1. 소재 확인
사용자가 소재를 함께 입력했으면 그대로 사용. 없으면 질문:
- 제품명, 타겟, 핵심문제, 감정, 신뢰근거, CTA, 유형(썰형/정보형), 글자수(기본600)

### 2. 파이프라인 실행 (Python 스크립트)
아래 스크립트를 Bash로 실행한다. 소재 정보를 스크립트 변수에 채워 넣는다.
중간에 사용자에게 아무것도 묻지 않는다.

```bash
python3 -m src.pipeline_v2.shorts \
  --product "{제품명}" \
  --target "{타겟}" \
  --problem "{핵심문제}" \
  --emotion "{감정}" \
  --trust "{신뢰근거}" \
  --cta "{CTA}" \
  --type "{유형}" \
  --length {글자수}
```

이어하기 (중간에 멈춘 프로젝트 재개):
```bash
python3 -m src.pipeline_v2.shorts --resume \
  --product "{제품명}" --target "{타겟}" --problem "{핵심문제}" \
  --emotion "{감정}" --trust "{신뢰근거}" --cta "{CTA}"
```

### 3. 결과 보고
스크립트 출력의 "최종 보고" 섹션을 사용자에게 보여준다.
- 대본 전문
- 품질 점수
- 리비전 횟수
- 훅 목록
- 생성된 파일 경로 (TTS가 있으면)
